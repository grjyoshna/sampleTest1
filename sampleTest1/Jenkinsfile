pipeline{
    
    agent any
   tools {
        maven 'Maven 3.6.3'
        jdk 'jdk8'
    }
    stages{
       stage ('Initialize') {
            steps {
                sh '''
                    echo "PATH = ${JAVA_HOME}"
                    echo "M2_HOME = ${M3}"
                '''
            }
        }
        
            stage('Checkout code') {
                 steps {
                 checkout scm
                 }
             }
        
        
        
       
       
      
    }
}
